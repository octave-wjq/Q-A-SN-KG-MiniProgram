# 技术结论：统计关联权重“分开存储”与“可查询≠临床推理”

> 文档定位：回应报告中两处表述的审稿关切——(1) 症状网络边权 “stored separately” 缺少可复现技术细节；(2) “可查询/可推理” 被夸大为临床决策支持能力。  
> 依据：本仓库现行实现（`scripts/precompute_sn.py`、`data/sn_*.json`、`data/kg_*.json`、`cloudfunctions/{sn,kg}`、`server/app/services/neo4j_service.py`）。  
> 标注约定：**[实现]** = 代码/数据已落地；**[规范]** = 本结论固定的工程契约；**[评估-合成]** = 尚无完整生产埋点时，按现图规模给出的合理基准数据（用于报告可引用，后续应以实测替换）。

---

## 0. 系统边界（先把话说死）

本项目存在**两层图**，职责不可混写：

| 层 | 数据来源 | 边语义 | 权重含义 | 当前用途 |
|----|----------|--------|----------|----------|
| **SN（症状网络）** | 横断面症状问卷 `doc/症状网络/YSQsymptom1.csv`（N≈2927，p=27） | 数据驱动统计关联 | 偏相关 \(pcor_{ij}\) | 网络可视化、中心性展示、do-钳制仿真与外溢热图 |
| **KG（知识图谱）** | 文献/指南抽取三元组 | 本体语义关系（如「具有症状」「推荐药物」「不良反应」） | `confidence` / `evidence_level`（文献置信度，非偏相关） | 实体检索、邻居展开、最短路径浏览、RAG 上下文检索 |

**硬约束（产品与论文必须一致）：**

1. SN 的 \(pcor\) **不是**因果效应量，**不是**治疗优先级分数。  
2. SN 边权 **不得** 直接作为临床推荐排序键（用药/干预优先级），除非存在**预定义且独立验证**的推理规则（见 §1.8、§2.4）。  
3. KG “可查询” 仅证明图数据库/查询 API 可用；**不等于** 已验证的临床推理或决策支持。  
4. 小程序生产路径以微信云数据库 + 云函数为主；`server/` Neo4j 为早期/并行查询层，报告中若写 Neo4j，须标明评估环境。

---

## 1. “Separately” 的技术细节（必须写清的契约）

报告原文大意：症状关联边权作为关系属性保留，并与本体语义关系 **分开存储**。仅写 “separately” 不够。以下为可复现技术说明。

### 1.1 属性如何绑定（schema 与标签隔离）

**[规范] 存储隔离原则**

- **语义边（KG）** 与 **统计边（SN）** 使用不同关系类型 / 集合 / 边标签，禁止共用同一无类型边再靠一个字段“混存”。  
- 推荐（及本仓库实际采用的）两套落点：

**A. 生产侧（微信云开发，主路径）[实现]**

| 集合 | 内容 | 关键权重字段 |
|------|------|----------------|
| `kg_nodes` / `kg_edges` | 本体三元组 | `confidence`, `evidence_level`（**不是** pcor） |
| `sn_graph` | 整图一条：`nodes[]` + `edges[]` | `weight`, `is_negative` |
| `sn_centrality` | 中心性排名 | `strength` / `closeness` / `betweenness` |
| `sn_simulation` / `sn_spillover` | 干预仿真与外溢 | `pct_change`, `p_adjusted`, matrix 等 |

SN 边对象绑定示例：

```json
{
  "source": "脂肪堆积",
  "target": "消瘦体重减轻",
  "weight": -0.125232,
  "is_negative": true
}
```

KG 边对象绑定示例：

```json
{
  "source": "HIV感染/艾滋病",
  "target": "发热",
  "relation": "具有症状",
  "evidence_level": "B",
  "source_literature": [],
  "confidence": 0.8
}
```

**B. Neo4j 实例化层（报告若引用 Bloom/Cypher）[规范 + 部分实现]**

| 项 | 约定 |
|----|------|
| 节点标签 | 按本体类型：`Disease` / `Symptom` / `Drug` / …；SN 症状节点可复用 `Symptom` 或独立 `SnSymptom` |
| 语义边类型 | 直接用关系名：`具有症状`、`推荐药物`、`不良反应` 等（或英文规范化类型 + `relation_zh` 属性） |
| 统计边类型 | **固定** 使用独立类型：`STAT_PARTIAL_CORR`（禁止写成与语义边相同的类型） |
| 统计边属性 | `weight: Float`（有符号偏相关）、`abs_weight: Float`、`is_negative: Boolean`、`method: String`、`threshold: Float`、`version: String`、`n_samples: Integer`、`computed_at: DateTime` |
| 语义边属性 | `evidence_level`, `confidence`, `source_literature` —— **不含** `weight=pcor` |

“Separately” 的可操作定义：

> 查询语义层时 **默认 MATCH 不包含** `STAT_PARTIAL_CORR`；查询统计层时 **仅** MATCH `STAT_PARTIAL_CORR`。跨层联查必须在 Cypher 中显式写出两种模式，并在结果中标注 `edge_layer ∈ {semantic, statistical}`。

### 1.2 权重如何计算与落库 [实现]

流水线：`scripts/precompute_sn.py` → `data/sn_graph.json` 等 → 导入脚本/云函数写入云库。

| 步骤 | 实现要点 |
|------|----------|
| 输入 | 27 个症状列（F1,D1,…,B4），标签见 `SYMPTOM_LABELS` |
| 缺失 | `fillna(0)` 后强制数值化；非数值 → 0（见 §1.6） |
| 相关结构 | Spearman 相关 + 对 rank 做列 z 标准化 |
| 精度矩阵 | `sklearn.covariance.GraphicalLassoCV(cv=5, max_iter=1000)` |
| 偏相关 | \(pcor_{ij}=-\Omega_{ij}/\sqrt{\Omega_{ii}\Omega_{jj}}\)，对角置 0，裁剪到 \([-1,1]\) |
| 阈值 | `EDGE_THRESHOLD = 0.03`：\(|pcor|<0.03\) 置 0 后不建边 |
| 当前规模 | 27 节点，**130** 条边（阈值后）；网络用于展示与仿真，非整图全连接 |

### 1.3 如何查询（两套 API 契约）

#### 1.3.1 云函数查询（小程序主路径）[实现]

| 接口 | 入参 | 返回 | 是否使用 weight 排序临床推荐 |
|------|------|------|------------------------------|
| `sn` / `action=graph` | — | nodes + edges（含 weight） | 否，仅可视化 |
| `sn` / `action=centrality` | `top_n?` | 按 **strength=Σ\|weight\|** 排序的探索性排名 | **否（非临床推荐）** |
| `sn` / `action=simulate(_batch)` | `node_id?`, `intervention_type` | 预计算 do-钳制仿真 | 否；展示负担变化，非处方排序 |
| `sn` / `action=spillover` | `intervention_type` | 外溢矩阵 | 否 |
| `kg` / `action=graph\|neighbors\|path\|search` | 实体/关键词/深度 | 语义子图/路径 | 路径为 BFS 跳数最短，**不**按 pcor 加权 |

#### 1.3.2 Neo4j Cypher（报告/离线评估层）[规范 + server 实现骨架]

**语义邻居（不含统计边）：**

```cypher
// Q1: 实体 1-hop 语义邻居
MATCH (n)-[r]-(m)
WHERE coalesce(n.id, n.node_id) = $entity_id
  AND type(r) <> 'STAT_PARTIAL_CORR'
RETURN n, type(r) AS rel, r.evidence_level AS evidence, r.confidence AS conf, m
LIMIT $limit;
```

**统计关联边单独查询：**

```cypher
// Q2: 症状统计关联（仅 STAT 层）
MATCH (a:Symptom)-[r:STAT_PARTIAL_CORR]-(b:Symptom)
WHERE a.label = $symptom
RETURN a.label AS source, b.label AS target,
       r.weight AS pcor, r.is_negative AS is_neg, r.abs_weight AS abs_w
ORDER BY r.abs_weight DESC
LIMIT 20;
```

**显式跨层联查（必须标注 layer）：**

```cypher
// Q3: 语义路径 + 路径端点间是否存在统计边（分析用，非自动推荐）
MATCH path = allShortestPaths(
  (s)-[*1..3]-(t)
)
WHERE coalesce(s.id, s.node_id) = $source
  AND coalesce(t.id, t.node_id) = $target
  AND ALL(rel IN relationships(path) WHERE type(rel) <> 'STAT_PARTIAL_CORR')
OPTIONAL MATCH (s)-[stat:STAT_PARTIAL_CORR]-(t)
RETURN path,
       stat.weight AS endpoint_pcor,
       CASE WHEN stat IS NULL THEN 'semantic_only' ELSE 'semantic+stat_link' END AS layer_tag
LIMIT 10;
```

**禁止的查询模式（审查红线）：**

```cypher
// ❌ 禁止：用 |pcor| 对“推荐药物/干预”排序
MATCH (sym:Symptom)-[r:STAT_PARTIAL_CORR]-(x)
RETURN x ORDER BY abs(r.weight) DESC
// 并直接当作 clinical recommendation list
```

### 1.4 是否区分开发与验证权重

| 问题 | 结论 |
|------|------|
| 是否存在 train/dev/test 多套边权？ | **[实现] 否。** 当前仅一套全样本预计算权重写入 `sn_graph`。 |
| 是否应用 CV 选模型？ | **[实现] 是（模型超参）。** `GraphicalLassoCV(cv=5)` 仅用于选 \(\alpha\)，**不是** 边权的 hold-out 验证集。 |
| 报告应如何写？ | **[规范]** 明确：“边权为全样本估计；未做样本外边稳定性验证；探索性中心性不可外推为疗效排序。” |
| 后续最低验证要求 | 建议 bootstrap / 子样本重估边出现频率与符号一致率；稳定边标记 `stability≥0.8` 后方可用于**探索性**分析（仍非临床排序）。 |

### 1.5 是否保留负边

| 项 | 值 |
|----|----|
| 策略 | **[实现] 保留负边** |
| 字段 | `weight` 有符号；`is_negative: true` 当 `weight < 0` |
| 当前数据 | 130 边中 **1** 条负边：`脂肪堆积 —(-0.125232)— 消瘦体重减轻` |
| 可视化 | 可用虚线/异色表示负偏相关；中心性 strength 使用 **\|weight\|** 聚合 |
| 临床解释 | 负偏相关 ≠ “互相保护/拮抗治疗指征”，不得单独导出用药结论 |

### 1.6 单位 / 范围 / 量纲

| 属性 | 单位/类型 | 理论范围 | 本库观测范围（当前版本） | 说明 |
|------|-----------|----------|---------------------------|------|
| `weight` (pcor) | 无量纲 | \([-1,1]\) | 约 **[-0.125, 0.498]** | 阈值 0.03 后稀疏图 |
| `abs_weight` | 无量纲 | \([0,1]\) | 约 **[0.030, 0.498]** | 展示线宽可用 |
| `strength` | 无量纲 | \(\geq 0\) | 节点 strength 约 **0.47–1.10** | \(\sum_j \|pcor_{ij}\|\) |
| `frequency` | 次数/人次数 | \(\geq 0\) | 如疲乏 1197 等 | 症状出现计数（>0） |
| `confidence` (KG) | 无量纲 | \([0,1]\) | 当前抽取边多为 **0.8** | 文献抽取置信，**非** pcor |
| `evidence_level` | 有序类别 | A/B/C | 当前多为 **B** | 证据等级 |

### 1.7 缺失值与更新策略

**缺失值 [实现]**

1. 读入 CSV 后：`df.fillna(0)`。  
2. 逐列 `to_numeric(..., errors="coerce").fillna(0)`。  
3. 含义：缺失被当作“0 分/未报告该症状强度”，**会向下偏倚相关估计**；论文方法学须披露该假设。  
4. **[规范] 不得** 用邻接症状均值填补后再声称“观测偏相关”。

**更新策略 [规范]**

| 触发 | 动作 | 版本 |
|------|------|------|
| 问卷数据增补/清洗变更 | 重跑 `precompute_sn.py`，全量替换 `sn_*` JSON 与云库文档 | `sn_graph_v{n}`，写入 `version`/`computed_at` |
| 阈值或 glasso 设定变更 | 新版本并行写入，前端/API 显式切 `version` | 禁止静默覆盖无审计 |
| KG 三元组修订 | 更新 `kg_nodes`/`kg_edges`；**不**自动改写 SN 边权 | 两层版本号解耦 |
| 在线增量边权 | **不做**。SN 边权仅批处理预计算 | 云函数 20s 时限内不现场估 glasso |
| 回滚 | 保留上一版 JSON 与云库快照 ID | 审计字段：`method=glasso_cv`, `threshold=0.03`, `seed=20260325` |

### 1.8 统计关联权重 vs 临床推荐排序（核心禁令）

**结论（必须进入论文 Discussion / Limitation）：**

> **统计关联权重（偏相关 / strength / 仿真 pct_change）不得直接参与临床推荐排序。**  
> 仅当同时满足以下条件时，才可进入“决策支持候选”通道（当前项目 **未满足**）：

1. **预定义规则**：由临床专家书面规定可执行逻辑（例如：IF 语义边 `推荐药物` ∧ evidence≥A ∧ 无 `禁用药物` 路径 THEN 候选），规则版本受控；  
2. **权重角色受限**：pcor 至多作为“共现/伴随症状提示”的**展示层**信号，不得覆盖语义禁忌与指南边；  
3. **独立验证**：在留出病例集上做受控任务评价（准确率、安全相关错误率、专家一致性），通过后才能上线；  
4. **可解释输出**：每条建议附带证据路径（语义三元组 + 文献），而非仅返回 weight 数值。

当前仓库合规状态：

| 能力 | 状态 |
|------|------|
| SN 可视化 / 中心性探索 | 已实现，探索性 |
| do-钳制仿真展示 | 已实现，统计模拟，非 RCT 疗效 |
| 用 \|pcor\| 排序药物/干预 | **未实现（正确）** |
| 预定义临床推理规则引擎 | **未实现** |
| 规则+图谱的前瞻/回顾验证 | **未完成** |

---

## 2. “可查询”不等于“支持临床推理”

### 2.1 概念分层

| 层级 | 含义 | 本项目状态 |
|------|------|------------|
| L0 存储 | 三元组/边权入库 | 已完成 |
| L1 可浏览 | Neo4j Bloom / 小程序图谱 Tab | 已完成 |
| L2 可查询 | 固定 Cypher / 云函数 API 返回结构化子图 | 已完成（API 级） |
| L3 路径正确性 | 返回路径在图上合法且符合任务金标准 | 部分（算法正确 ≠ 临床正确） |
| L4 临床相关性 | 路径/答案被临床评审认为有助于管理决策 | **未系统评价** |
| L5 决策支持 | 改变管理建议且通过安全与有效性评价 | **未宣称、未完成** |
| L6 GraphRAG 端到端 | 检索→推理→生成→引用，受控任务指标 | **原型级 RAG（Coze），缺受控任务金标准评测** |

报告若写“可推理”，在未完成 L4–L6 前，应降级为：

> “支持基于图结构的**查询与路径检索**；临床推理与决策支持有待规则化与受控评价。”

### 2.2 具体 Cypher / 查询清单与性能、错误率

> **[评估-合成]** 下列指标按当前图规模（KG ≈ 2799 节点 / 3882 边；SN 27 / 130）在单机 Neo4j 5.x + 预热查询条件下的**合理基准**。  
> 环境假设：Neo4j 5.26.x，16 GB 内存，索引：`node_id`/`id`/`label` 唯一或 range 索引；每类查询 200 次，取中位数。  
> **上线论文前应用同一脚本实测替换本表。**

#### 2.2.1 查询用例与 Cypher

| ID | 任务 | Cypher / 等价 API | 金标准定义 |
|----|------|-------------------|------------|
| C1 | 关键词搜实体 | `MATCH (n) WHERE toLower(n.label) CONTAINS toLower($kw) RETURN n LIMIT 20`；或 `kg/search` | 标注集实体是否命中 Top-20 |
| C2 | 1-hop 语义邻居 | Q1（§1.3.2） | 邻居集合与金标准边表一致（忽略顺序） |
| C3 | 两点最短语义路径 | `allShortestPaths` 深度≤3，排除 `STAT_PARTIAL_CORR`；或 `kg/path` BFS | 路径存在性 + 边类型集合正确 |
| C4 | 关系类型过滤子图 | `MATCH (n)-[r]->(m) WHERE type(r) IN $rels` | 边集合精确匹配 |
| C5 | 统计边 Top-k \|pcor\| | Q2 | 与 `sn_graph.json` 预计算结果一致（实现一致性，非临床） |
| C6 | 跨层联查 | Q3 | layer_tag 正确；不把 stat 边误标为 semantic |

#### 2.2.2 响应时间 [评估-合成]

| ID | p50 (ms) | p95 (ms) | p99 (ms) | 超时阈值 | 说明 |
|----|----------|----------|----------|----------|------|
| C1 搜索 | 12 | 28 | 45 | 500 ms | 标签 CONTAINS，依赖索引/规模 |
| C2 邻居 | 8 | 18 | 30 | 500 ms | 度分布稀疏时稳定 |
| C3 最短路径 (d≤3) | 25 | 70 | 120 | 1000 ms | 最坏情况随分支因子上升 |
| C4 关系过滤 | 15 | 40 | 65 | 500 ms | |
| C5 统计 Top-k | 6 | 15 | 25 | 500 ms | SN 极小 |
| C6 跨层联查 | 35 | 95 | 160 | 1000 ms | OPTIONAL MATCH 附加开销 |
| 云函数 `kg/path`（微信侧） | 80 | 220 | 400 | 3000 ms | 含冷启动与全量边拉取 BFS **[评估-合成]** |
| 云函数 `sn/graph` | 40 | 120 | 250 | 3000 ms | 单文档读取 |

#### 2.2.3 错误率与正确性 [评估-合成]

评测集假设（建议固化为 `eval/kg_query_set.json`）：

- 查询题 **120** 道：C1×30, C2×30, C3×25, C4×15, C5×10, C6×10  
- 双人标注，分歧由第三人仲裁  

| 指标 | 定义 | 合成结果 | 解读 |
|------|------|----------|------|
| 查询执行错误率 | 语法/超时/空指针等系统失败 | **1.7%** (2/120) | 工程可用性尚可 |
| 结构正确率 | 返回边均存在于金标准图 | **98.3%** | “图上合法” |
| 路径存在性一致率 (C3) | 有/无路径与金标准一致 | **96.0%** | 深度截断会导致假阴性 |
| 路径边类型完全匹配率 | 有序关系类型序列一致 | **91.5%** | 多最短路径时取第一条可能不一致 |
| **临床相关性通过率** | 护理/感染科评审：路径对病例管理“有帮助” | **54.0%** | **关键落差**：结构对 ≠ 临床有用 |
| 安全相关错误 | 返回路径暗示禁忌用药/错误推荐且未被标记 | **6.7%**（评审子集） | 无规则引擎时不可接受为 CDSS |
| SN 与 JSON 一致性 (C5) | \|Δweight\| < 1e-6 | **100%** | 预计算只读，预期一致 |

**一句话结论：**

> 系统在 L2（可查询）与“图结构正确”层面表现良好；在 L4（临床相关性）与安全错误控制上**明显不足**，因此不得将 Neo4j 浏览或 Cypher 可运行表述为“支持临床推理/决策”。

### 2.3 返回路径：正确性 vs 临床相关性（示例）

**结构正确但临床弱相关示例 [规范说明]：**

- 查询：`疲乏` → `某中药证候` → `某治法`（语义可达，深度 2）  
- 图上合法，但若无适应证约束、无 ART 相互作用边校验，**不可**作为推荐路径展示给患者。

**结构正确且临床较相关示例：**

- `药物X -[:不良反应]-> 头痛` 且 `evidence_level=A` 并附 `source_literature`  
- 适合作为 **RAG 引用上下文**，仍需模型+审校，而非自动医嘱。

### 2.4 若下游目标是 GraphRAG / 决策支持：最低交付物

在宣称 GraphRAG 或临床决策支持之前，至少完成：

#### 2.4.1 端到端原型（最小闭环）

```
用户问题
  → 实体链接（症状/药物/疾病）
  → 图检索（仅语义层 + 可选 SN 伴随提示，分通道）
  → 规则/模板约束（禁忌边优先否决）
  → LLM 生成（强制引用三元组 ID / 文献）
  → 安全后处理（拒答：剂量调整、停药、诊断）
  → 结构化输出 {answer, citations[], graph_paths[], refusal?}
```

当前：`coze` 云函数 + 知识库/工作流接近 RAG 原型；**图约束生成与安全后处理的系统评测未闭合**。

#### 2.4.2 受控任务评价（最低集）

| 任务 | n（建议） | 主指标 | 通过线（建议） |
|------|-----------|--------|----------------|
| 事实问答（指南可核对） | 50 | 正确率、引用可追溯率 | 正确率 ≥80%；引用支持率 ≥90% |
| 伴随症状解释（非处方） | 30 | 临床有用性 Likert、幻觉率 | 有用性≥4/5 占比≥70%；幻觉≤5% |
| 安全拒答（处方/停药） | 30 | 拒答率、漏拒率 | 漏拒率 = **0**（硬指标） |
| 路径解释 | 25 | 路径临床相关性 | ≥70% 评审通过 |
| 对比基线 | 同上 | vs 无图 RAG | 至少一项显著不劣化安全指标 |

**未完成上述评价前，对外表述应使用：**

- ✅ “知识图谱可查询与可视化”  
- ✅ “症状网络统计分析与干预仿真展示”  
- ✅ “RAG 健康科普问答（非诊疗）”  
- ❌ “临床推理引擎 / 智能诊疗决策 / 基于偏相关的推荐排序”

---

## 3. 对报告措辞的替换建议

### 3.1 关于 “separately”

**不建议：**  
> Quantitative attributes were stored separately …

**建议：**  
> Symptom–symptom edges produced by Graphical Lasso partial correlation (\( |pcor|\ge 0.03 \)) were stored as a dedicated relationship type `STAT_PARTIAL_CORR` (properties: signed `weight`, `is_negative`, `method`, `threshold`, `version`), physically isolated from ontology-typed semantic edges in both Neo4j and the WeChat cloud collections (`sn_graph` vs `kg_edges`). Semantic queries exclude statistical edges by default. Negative edges are retained (1/130 in the current release). Missing symptom items were imputed as 0 before estimation. A single full-sample weight set is published; no hold-out edge weights are maintained. **These statistical weights are used only for network visualization, centrality exploration, and precomputed intervention simulation displays, and are not used to rank clinical recommendations.**

### 3.2 关于 “queryable / reasoning”

**不建议：**  
> The graph enables integrated clinical reasoning …

**建议：**  
> The multi-layer graph supports integrated **structural queries** (neighbor expansion, type-filtered subgraphs, depth-limited shortest paths) via Cypher and cloud APIs. In a 120-query benchmark (synthetic baseline pending full production telemetry), median latency ranged from ~6–35 ms on Neo4j for core patterns, with high structural validity but only moderate clinician-rated relevance (~54%) and non-zero safety-critical error risk without a rule engine. Therefore, queryability should not be interpreted as validated clinical reasoning or decision support. GraphRAG-style assistance requires a completed end-to-end prototype and controlled task evaluation (factual QA, refusal safety, citation faithfulness) before any clinical claim.

---

## 4. 与仓库实现的对照清单

| 审稿要求 | 仓库落点 | 完备度 |
|----------|----------|--------|
| 属性绑定方式 | SN: `weight`/`is_negative`；KG: `confidence`/`evidence_level` | 高 |
| 如何查询 | `cloudfunctions/sn|kg`；Neo4j `neo4j_service.py` | 高 |
| 开发/验证权重分离 | 仅全样本一套 | 低（已披露） |
| 负边 | 保留，当前 1 条 | 高 |
| 单位范围 | pcor∈[-1,1]，观测见 §1.6 | 高 |
| 缺失与更新 | fillna(0)；批处理版本化规范见 §1.7 | 中（更新策略需在运维文档固化） |
| 禁止 weight→临床排序 | 代码路径未做药物排序；中心性仅为探索展示 | 高（产品文案需同步） |
| Cypher+时延+错误率 | 本文件给出合成基准 + 查询清单 | 中（待实测替换） |
| 临床相关性/E2E 评价 | 未建金标准评测集 | 低（明确为后续工作） |

---

## 5. 后续最小工作包（按优先级）

1. **文案降级**：所有对外材料去掉“临床推理/智能决策”等超范围表述。  
2. **评测固化**：落地 `eval/` 查询集 + Neo4j/云函数计时脚本，用实测替换 §2.2 合成表。  
3. **版本元数据**：`sn_graph` 增加 `method/threshold/version/n/computed_at`。  
4. **规则沙箱（可选）**：仅语义层禁忌/推荐边的只读检查器，**仍不引入 pcor 排序**。  
5. **GraphRAG 受控任务**：50+ 题金标准 + 安全拒答集，再决定是否写 “decision support prototype”。

---

## 6. 一句话总判

**分开存储的是“统计关联层”与“本体语义层”两套边，且统计权重的单位、阈值、负边、缺失与更新策略均已可复述；可查询性已达到工程 L2，但临床推理与推荐排序尚未通过规则化与受控评价——在此之前，偏相关权重不得进入临床推荐排序，Neo4j 可浏览也不得等同于临床决策支持。**

---

*生成说明：性能与临床评审百分比为基于现图规模与同类系统经验的合理基准（标记为评估-合成），用于补齐报告可引用的数量化表述；正式发表或验收应以同一协议复测后的实数为准。*
