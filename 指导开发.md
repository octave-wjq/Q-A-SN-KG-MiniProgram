# 艾滋病健康管理小程序 - 指导开发文档

## 目录
1. [项目背景与目标](#1-项目背景与目标)
2. [系统架构与技术栈](#2-系统架构与技术栈)
3. [核心功能与API设计](#3-核心功能与api设计)
4. [数据结构定义](#4-数据结构定义)
5. [两张图的设计与交互](#5-两张图的设计与交互)
6. [开发步骤与里程碑](#6-开发步骤与里程碑)
7. [待澄清问题](#7-待澄清问题)

---

## 1. 项目背景与目标

### 1.1 项目背景
复旦大学护理学院项目。通过构建"**症状网络 + 知识图谱 + RAG大模型**"的智能交互系统，为艾滋病患者提供居家健康教育与自我管理支持。

核心思路：
- **症状网络**（数据驱动）：基于GGM偏相关网络，发现核心症状、共现结构、预测伴随症状、模拟干预效应
- **知识图谱**（语义驱动）：基于文献三元组，解释因果关系、提供干预路径
- **RAG问答**：检索增强生成，提供循证答案
- 三者在问答层编排协同，但**数据、接口、展示、推理链路完全分离**

### 1.2 项目约束
- **团队**：1人开发 + AI辅助
- **目标用户**：艾滋病患者（居家健康管理）
- **使用规模**：日活50-100人
- **交付标准**：功能完整 + 达到可发文章水平
- **无硬性deadline**，按里程碑顺序推进

### 1.3 核心功能清单

| 功能 | 优先级 | 说明 |
|------|--------|------|
| RAG健康知识问答 | P0 | 输入问题→检索文献+图谱→大模型生成答案+来源+证据等级 |
| 症状网络图展示 | P0 | 节点=症状，边=偏相关强度，支持缩放/搜索/高亮 |
| 知识图谱图展示 | P0 | 有向图，展示因果/干预关系，支持展开/路径查询 |
| 干预仿真展示 | P0 | do-钳制仿真结果、核心症状排名、外溢效应热图 |
| 症状输入与管理建议 | P0 | 结构化表单录入症状→触发网络分析+图谱推理→分级建议输出 |
| 用户模块 | P0 | 登录/注册、基本信息、既往病史 |
| 健康管理工具 | P1 | 用药提醒、复诊提醒、运动记录、饮食记录 |
| 用户反馈 | P1 | 标注答案质量（有帮助/不准确），无法回答时提示转人工 |

### 1.4 关键业务流程

**流程1：健康知识问答**
```
用户输入问题 → RAG检索（文献库 + 知识图谱）→ 大模型生成答案
→ 返回答案 + 原文引用 + 证据等级 → 系统推荐查看对应图谱
→ 用户可选择查看[症状结构视图]或[原因与干预视图]
```

**流程2：症状录入 → 管理建议**
```
用户填写基本信息 → 录入症状（结构化表单）→ 症状标准化
→ 症状网络分析（预测伴随症状 + 干预仿真）
→ 知识图谱推理（因果关系 + 干预路径）
→ 大模型整合生成管理建议 → 分级输出（紧急/随访/日常）+ 循证依据
```

**流程3：查看图谱与仿真**
```
用户在对话界面点击[症状结构视图]或[原因与干预视图]
→ 症状网络视图：网络图 + 核心症状排名 + 干预仿真结果 + 外溢效应热图
→ 知识图谱视图：有向关系图 + 节点展开 + 路径查询
→ 支持节点点击、缩放、搜索、路径高亮
```

### 1.5 数据资源

| 数据 | 格式 | 说明 |
|------|------|------|
| 中文文献 | json/conll/txt | 已清洗标注，约60-70篇 |
| 英文文献 | txt | 仅原文，需进一步清洗（约1/3待处理） |
| 症状网络数据 | csv（个体×症状矩阵） | R语言处理，GGM偏相关网络 |
| 知识图谱三元组 | 实体-关系-实体 | 文献提取，需构建/补充 |

**关键关系类型**：疾病-症状（表现为）、症状-阶段（发生于）、疾病-干预（缓解/改善）、药物-症状（治疗/诱发）、干预-阶段（适用于）、疾病-检测（检测出）、症状-检测（表现在）、疾病-传播方式（导致）、症状-症状（共现/因果）

### 1.6 非功能性要求

| 指标 | 要求 |
|------|------|
| 问答响应时间 | < 3秒 |
| 图谱加载时间 | < 2秒 |
| 并发用户数 | 50人（峰值） |
| 隐私合规 | 患者信息加密存储，日志脱敏 |
| 测试覆盖 | 单元测试 ≥ 90% |
| 大模型准确率 | ≥ 85%，F1 ≥ 0.80 |

---

## 2. 系统架构与技术栈

### 2.1 架构概览（纯云开发）

```
┌─────────────────────────────────────────────┐
│              微信小程序（前端）                │
│  问答界面 │ 症状网络图 │ 知识图谱图 │ 健康管理  │
└──────────────────┬──────────────────────────┘
                   │ wx.cloud.callFunction()
┌──────────────────▼──────────────────────────┐
│              微信云开发                       │
│  ┌────────┐ ┌──────┐ ┌────┐ ┌──────┐       │
│  │ coze   │ │  kg  │ │ sn │ │health│ ...   │
│  │(问答)  │ │(图谱)│ │(网络)│ │(管理)│       │
│  └───┬────┘ └──┬───┘ └─┬──┘ └──┬───┘       │
│      │         │       │       │            │
│  ┌───▼───┐  ┌──▼───────▼───────▼──┐        │
│  │Coze   │  │     云数据库         │        │
│  │API    │  │  kg_nodes/kg_edges  │        │
│  │(外部) │  │  sn_graph/sn_sim   │        │
│  └───────┘  │  users/health_*    │        │
│             └─────────────────────┘        │
└─────────────────────────────────────────────┘
```

**无云服务器**。所有后端逻辑在云函数（Node.js）中完成。
- 问答：云函数直接调 Coze API
- 知识图谱：三元组存云数据库，云函数做查询/遍历
- 症状网络：本地 Python 预计算，结果存云数据库，云函数只做查询
- 用户/健康管理：云数据库 CRUD

### 2.2 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| **前端** | 微信小程序原生 + AntV G6 | 图谱可视化用G6，ECharts做热图/图表 |
| **后端** | 微信云函数（Node.js） | 零运维，自动扩缩 |
| **数据库** | 微信云数据库（MongoDB-like） | 知识图谱、症状网络、用户数据 |
| **RAG问答** | Coze API（外部） | 搭建工作流，直接API返回 |
| **预计算** | Python (numpy/scipy/sklearn/networkx) | 本地运行，输出JSON导入云数据库 |
| **图谱可视化** | AntV G6 + ECharts | G6做网络/图谱图，ECharts做柱状图/热图 |

**不再需要**：FastAPI、Neo4j、PostgreSQL、ChromaDB、Redis、Docker、LangChain

---

## 3. 核心功能与云函数设计

> 所有后端接口通过云函数实现，前端统一用 `callCloud('函数名', {action, ...params})` 调用。

### 3.1 用户模块（云函数: `login` / `user`）

| 云函数 | action | 功能 | 入参 | 出参 |
|--------|--------|------|------|------|
| `login` | — | 微信登录 | — | `{openid, appid}` |
| `user` | `getProfile` | 获取用户信息 | — | `{user_info, medical_history}` |
| `user` | `updateProfile` | 更新用户信息 | `{user_info}` | `{success}` |

### 3.2 问答模块（云函数: `coze`）

| action | 功能 | 入参 | 出参 |
|--------|------|------|------|
| `chat` | 提交问题（调Coze API） | `{question, user_id?}` | `{answer, sources[], evidenceLevel}` |
| `feedback` | 用户反馈 | `{qa_id, feedback_type, comment}` | `{success}` |
| `history` | 历史问答 | `{page, page_size}` | `{qa_list[], total}` |

### 3.3 症状网络模块（云函数: `sn`）

> 数据全部预计算，云函数只做查询。

| action | 功能 | 入参 | 出参 |
|--------|------|------|------|
| `graph` | 获取症状网络图数据 | — | `{nodes[], edges[]}` |
| `centrality` | 核心症状排名 | `{top_n?}` | `{rankings[]}` |
| `simulate` | 单节点干预仿真结果 | `{node_id, intervention_type}` | `{baseline, intervened, pct_change, p_value, ci_95}` |
| `simulate_batch` | 全节点仿真排名 | `{intervention_type}` | `{rankings[]}` |
| `spillover` | 外溢效应矩阵 | `{intervention_type}` | `{matrix[][], node_names[]}` |

`intervention_type`：`"alleviate"`（缓解，mu-2SD）或 `"aggravate"`（加重，mu+2SD）

### 3.4 知识图谱模块（云函数: `kg`）

| action | 功能 | 入参 | 出参 |
|--------|------|------|------|
| `graph` | 获取图谱数据 | `{entity_ids[]?, relation_types[]?}` | `{nodes[], edges[]}` |
| `neighbors` | 节点邻居 | `{entity_id}` | `{nodes[], edges[]}` |
| `path` | 实体间路径（BFS） | `{source, target, max_depth?}` | `{paths[]}` |
| `search` | 模糊搜索节点 | `{keyword}` | `{results[]}` |

### 3.5 症状输入与管理建议（云函数: `coze` + `sn`）

| 步骤 | 调用 | 说明 |
|------|------|------|
| 1. 提交症状 | `sn.graph` + 前端匹配 | 标准化症状名称 |
| 2. 预测伴随症状 | `sn.simulate` | 基于预计算结果 |
| 3. 生成管理建议 | `coze.chat` | 将症状+预测结果拼入prompt，让Coze生成建议 |

分级响应策略：
- 紧急就医：危险信号（如高热不退、意识模糊等）
- 短期随访：需门诊复查的情况
- 日常管理：生活方式、营养、心理支持建议

### 3.6 健康管理模块（云函数: `health`）

| action | 功能 |
|--------|------|
| `medication_add/get/delete` | 用药提醒管理 |
| `appointment_add/get/delete` | 复诊提醒管理 |
| `exercise_add/get` | 运动记录 |
| `diet_add/get` | 饮食记录+营养分析 |

---

## 4. 数据结构定义

### 4.1 症状网络节点/边

**节点（Symptom Node）**
```json
{
  "id": "string",
  "label": "string",
  "centrality": 0.72,
  "frequency": 150,
  "metadata": {
    "description": "症状描述文本",
    "severity_range": "1-10"
  }
}
```

**边（Symptom Edge）**
```json
{
  "source": "焦虑",
  "target": "情绪低落",
  "weight": 0.41,
  "relation_type": "co-occurrence",
  "confidence": 0.85
}
```

### 4.2 知识图谱节点/边

**节点（KG Node）**
```json
{
  "id": "string",
  "label": "string",
  "type": "疾病|症状|干预|药物|检测|传播方式|社会因素",
  "properties": {
    "description": "string",
    "aliases": ["别名1"],
    "evidence_level": "A|B|C"
  }
}
```

**边（KG Edge）**
```json
{
  "source": "歧视",
  "target": "抑郁",
  "relation": "增加风险",
  "direction": "directed",
  "evidence_level": "B",
  "source_literature": ["文献1.pdf"],
  "confidence": 0.78
}
```

### 4.3 干预仿真结果结构

**单节点仿真结果**
```json
{
  "node_id": "焦虑",
  "intervention_type": "alleviate",
  "baseline_burden": 12.5,
  "intervened_burden": 9.8,
  "pct_change": -21.6,
  "p_value": 0.001,
  "p_adjusted": 0.003,
  "ci_95": [9.2, 10.4],
  "valid": true
}
```

**外溢效应矩阵**
```json
{
  "node_names": ["焦虑", "情绪低落", "失眠", "..."],
  "matrix": [
    [0.0, -0.32, -0.18, "..."],
    [-0.28, 0.0, -0.15, "..."]
  ],
  "intervention_type": "alleviate"
}
```

### 4.4 问答结果结构

```json
{
  "qa_id": "string",
  "question": "string",
  "answer": "string",
  "sources": [
    {
      "title": "文献标题",
      "evidence_level": "A",
      "excerpt": "相关段落摘录",
      "url": "string"
    }
  ],
  "evidence_level": "A",
  "graph_recommendation": "symptom_network|knowledge_graph|null",
  "timestamp": "2026-03-13T10:00:00Z"
}
```

### 4.5 症状输入结构

```json
{
  "symptoms": [
    {
      "symptom_name": "头痛",
      "onset_time": "2026-03-10",
      "duration": "3天",
      "severity": 6
    }
  ],
  "medical_history": {
    "existing_conditions": ["HIV"],
    "current_medications": ["替诺福韦"],
    "allergies": []
  }
}
```

---

## 5. 两张图的设计与交互

### 5.1 核心约束：两张图必须完全分离

| 维度 | 症状网络图 | 知识图谱图 |
|------|-----------|-----------|
| 数据来源 | 个体×症状矩阵（统计分析） | 文献/指南/专家知识（三元组） |
| 驱动方式 | 数据驱动 | 语义驱动 |
| 核心作用 | 发现结构（核心症状、共现、预测） | 解释机制（因果、干预路径） |
| 边的含义 | 偏相关强度（无向，粗细表示） | 语义关系（有向箭头，标签标注） |
| 布局 | 力导向（Force-Directed） | 层次布局（Dagre） |
| 独立接口 | 云函数 `sn` | 云函数 `kg` |

**协同逻辑**：症状网络 = 结构发现，知识图谱 = 语义解释，LLM = 语言整合。三者在问答层编排，但数据、接口、展示完全独立。

### 5.2 症状网络图

**可视化方案**：AntV G6，力导向布局
- 节点大小：根据中心性指标动态调整
- 边粗细：根据偏相关权重动态调整
- 节点颜色：可按症状簇分色

**交互功能**：
1. 缩放与平移（鼠标滚轮/拖拽）
2. 节点点击：显示症状详情（描述、频次、中心性排名）
3. 节点搜索：输入症状名称，高亮对应节点
4. 路径高亮：点击节点后高亮直接邻居
5. 中心性筛选：根据阈值筛选显示节点

**仿真展示区域**（症状网络视图内的子页面/Tab）：
- Tab1：网络图（默认）
- Tab2：核心症状排名（柱状图，按中心性排序）
- Tab3：干预仿真结果（折线图+95%CI误差棒，参考R代码Figure2）
- Tab4：外溢效应热图（ECharts热力图，参考R代码Figure3）

**性能优化**：节点数>100时仅显示中心性>阈值的节点

### 5.3 知识图谱图

**可视化方案**：AntV G6，层次布局（Dagre）
- 节点颜色：按实体类型区分（疾病/症状/干预/药物/社会因素）
- 边样式：有向箭头 + 边标签显示关系类型
- 边颜色：可按证据等级区分

**交互功能**：
1. 缩放与平移
2. 节点点击：显示实体详情（描述、证据等级、来源文献）
3. 节点展开：点击展开1-hop上下游关系
4. 路径查询：选择两个节点，查询并高亮最短路径
5. 关系筛选：按关系类型筛选显示边

**性能优化**：初始仅加载核心节点，按需动态加载邻居

### 5.4 图谱切换与推荐

**界面设计**：对话界面旁设置两个Tab按钮
- [症状结构视图]（症状网络图 + 仿真）
- [原因与干预视图]（知识图谱图）

**推荐逻辑**：系统根据用户问题自动高亮推荐其中一个，用户可手动切换。

---

## 6. 开发步骤与里程碑

### 6.1 里程碑规划

> 1人开发+AI辅助，无硬性deadline，按顺序推进。纯云开发架构。

| 里程碑 | 目标 | 预估工作量 | 关键产出 |
|--------|------|-----------|----------|
| **M1: 基础骨架** | 小程序+云函数跑通 | ~3工作日 | 项目结构、云开发环境、基础云函数 |
| **M2: 问答跑通** | Coze问答端到端可用 | ~3工作日 | coze云函数、对话界面完善 |
| **M3: 数据预计算** | 症状网络+KG数据就绪 | ~8工作日 | Python预计算脚本、JSON导入云数据库 |
| **M4: 症状网络展示** | 网络图+仿真展示 | ~8工作日 | sn云函数、G6网络图、ECharts图表 |
| **M5: 知识图谱展示** | KG图展示+交互 | ~6工作日 | kg云函数、G6有向图、路径查询 |
| **M6: 症状管理** | 症状输入+管理建议 | ~5工作日 | 症状表单、分级建议 |
| **M7: 健康工具** | 提醒+记录功能 | ~5工作日 | health云函数、各子页面 |
| **M8: 测试与发布** | 测试+测评+上线 | ~8工作日 | 测试、模型测评、小程序提审 |

**总预估**：~46工作日（约2.5个月，弹性调整）

### 6.2 M1: 基础骨架（已完成）

| 任务 | 状态 |
|------|------|
| 小程序5个Tab页面骨架 | ✅ |
| 云函数 login / user | ✅ |
| 前端 api.js（callCloud / callCoze） | ✅ |
| project.config.json 配置 | ✅ |

### 6.3 M2: 问答跑通

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 创建 coze 云函数 | `cloudfunctions/coze/` | 调 Coze API 返回答案 |
| 前端 callCoze 改为 callCloud | api.js 更新 | 不再依赖 callServer |
| 问答对话界面完善 | qa 页面 | 输入→答案+来源+证据等级 |
| 问答历史存储 | 云数据库 qa_history | 历史记录可查 |
| 用户反馈功能 | 反馈按钮 | 标注有帮助/不准确 |

### 6.4 M3: 数据预计算

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 编写 `scripts/precompute_sn.py` | 症状网络预计算脚本 | 输出 sn_graph/centrality/simulation/spillover JSON |
| 验证预计算结果与R代码一致 | 对比报告 | 关键指标误差<1% |
| 准备知识图谱三元组数据 | kg_nodes.json + kg_edges.json | 格式正确，关系类型完整 |
| 编写 `scripts/import_to_cloud.js` | 云数据库导入脚本 | 所有数据成功导入 |
| 导入症状网络数据到云数据库 | sn_* 集合 | 云函数可查询 |
| 导入知识图谱数据到云数据库 | kg_nodes + kg_edges 集合 | 云函数可查询 |

### 6.5 M4: 症状网络展示

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 创建 sn 云函数 | `cloudfunctions/sn/` | 支持 graph/centrality/simulate/spillover action |
| 前端：症状网络图（G6力导向） | 网络图页面 | 显示网络，支持缩放/点击/搜索 |
| 前端：核心症状排名图 | 柱状图 | ECharts柱状图展示 |
| 前端：干预仿真结果图 | 折线图+CI | 参考R代码Figure2样式 |
| 前端：外溢效应热图 | 热力图 | ECharts热力图展示 |

### 6.6 M5: 知识图谱展示

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 创建 kg 云函数 | `cloudfunctions/kg/` | 支持 graph/neighbors/path/search action |
| 前端：知识图谱图（G6 Dagre布局） | KG图页面 | 有向箭头，关系标签，按类型着色 |
| 前端：节点展开+路径查询交互 | 交互功能 | 点击展开、路径高亮 |
| 前端：图谱切换按钮+推荐高亮 | 切换功能 | 两个Tab，自动推荐高亮 |

### 6.7 M6: 症状管理

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 症状标准化匹配 | 前端匹配逻辑 | 输入文本→匹配标准症状 |
| 管理建议生成 | 调 coze 云函数 | 将症状+预测拼入prompt |
| 前端：症状输入表单 | 表单页面 | 结构化录入，多症状支持 |
| 前端：管理建议展示 | 建议页面 | 分级显示+循证依据 |

### 6.8 M7: 健康工具

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 创建 health 云函数 | `cloudfunctions/health/` | 支持提醒/记录 CRUD |
| 用药提醒 + 前端 | 提醒功能 | 设置/查看/删除提醒 |
| 复诊提醒 + 前端 | 提醒功能 | 设置/查看/删除提醒 |
| 运动记录 + 前端 | 记录功能 | 记录运动，查看历史 |
| 饮食记录 + 前端 | 记录功能 | 记录饮食，营养分析 |

### 6.9 M8: 测试与发布

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 云函数功能测试 | 测试记录 | 所有 action 测试通过 |
| 前端功能测试 | 测试记录 | 核心流程无bug |
| 性能测试 | 测试报告 | 问答<3秒，图谱加载<2秒 |
| 构建测评数据集 | 100+问答对 | 含标准答案 |
| 模型测评 | 测评报告 | 准确率≥85%，F1≥0.80 |
| 敏感信息加密 | 安全措施 | 患者信息加密存储 |
| 小程序提审发布 | 上线 | 审核通过 |

---

## 7. 待澄清问题

### 7.1 数据相关

| ID | 问题 | 影响 | 当前假设 |
|----|------|------|----------|
| Q1 | 英文文献清洗在哪个阶段完成？ | Coze知识库质量 | M3阶段完成 |
| Q2 | 症状网络数据是否包含时间序列？ | 网络计算方式 | 仅共现矩阵（个体×症状） |
| Q3 | 知识图谱三元组是已有还是需自动提取？ | 构建工作量 | 需从文献中提取 |
| Q4 | 文献证据等级是否已标注？ | 排序逻辑 | 需自动判断 |

### 7.2 功能相关

| ID | 问题 | 影响 | 当前假设 |
|----|------|------|----------|
| Q5 | 管理建议分级标准？ | 建议输出 | 症状严重程度阈值 + Coze prompt |
| Q6 | 症状预测是否考虑个体差异？ | 预测准确率 | 仅基于群体统计（预计算） |

### 7.3 技术相关

| ID | 问题 | 影响 | 当前假设 |
|----|------|------|----------|
| Q7 | Coze Bot 工作流如何搭建？ | 问答质量 | 待搭建 |
| Q8 | 云数据库免费额度是否够用？ | 成本 | 日活50-100应该够 |
| Q9 | 知识图谱是否需动态更新？ | 维护成本 | 手动更新，重新导入 |

### 7.4 合规与发文

| ID | 问题 | 影响 | 当前假设 |
|----|------|------|----------|
| Q10 | 是否需要伦理审查？ | 合规 | 需要 |
| Q11 | 目标期刊？ | 测评标准 | 中文核心期刊 |
| Q12 | 是否需要对比实验？ | 论文质量 | 需要，与通用大模型对比 |

---

## 附录：关键风险

| 风险 | 应对 |
|------|------|
| Coze API不稳定 | 增加重试+本地兜底回复 |
| 知识图谱构建困难 | 简化关系类型，半自动提取 |
| R仿真移植Python精度差异 | 对比验证，确保结果一致 |
| 测评指标不达标 | 优化Coze工作流prompt，增加知识库数据 |
| 小程序审核不通过 | 提前了解规则，备用H5方案 |
| 云函数20s超时 | 大数据查询预计算，避免实时计算 |

---

**文档版本**：v3.0
**最后更新**：2026-03-19
**更新说明**：架构从云服务器切换为纯云开发（云函数+云数据库），去掉FastAPI/Neo4j/Docker依赖，症状网络全预计算，总工期从60缩减到46工作日
